import 'package:flutter/material.dart';

/// 占位：原生 Flutter 弹窗已由 WebView 版替代。
class ResponsiveTaskSheet extends StatelessWidget {
  const ResponsiveTaskSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}